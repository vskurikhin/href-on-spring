application:
  r2dbc:
    host: "localhost"
    database: "db"
    username: "dbuser"
    password: "password"

logging:
  level:
    root: WARN
    org.springframework:
      web: OFF
      data.r2dbc: OFF

server:
  port: 8002