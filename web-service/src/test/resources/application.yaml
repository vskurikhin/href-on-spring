application:
  locale: "en_US"

  r2dbc:
    host: localhost
    database: "db"
    username: "dbuser"
    password: "password"

  services:
    departments:
      host: localhost
      port: 8002
    employees:
      host: localhost
      port: 8003
    locations:
      host: localhost
      port: 8001

logging.level.org.springframework.web: INFO
