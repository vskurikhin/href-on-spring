application:
  services:
    departments:
      host: localhost
      port: 8002
    employees:
      host: localhost
      port: 8003
    locations:
      host: localhost
      port: 8001

logging.level.org.springframework.web: INFO
